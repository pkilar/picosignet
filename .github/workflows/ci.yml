name: ci

on:
  push:
    branches: [main, master]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  rust-host:
    name: rust host (fmt, clippy, test)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --all -- --check
      - run: cargo clippy --workspace --exclude hsm-fw --all-targets -- -D warnings
      - run: cargo test
      - run: bash tests/golden/verify_golden.sh

  rust-firmware:
    name: firmware cross-build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: thumbv6m-none-eabi
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy -p hsm-fw --target thumbv6m-none-eabi -- -D warnings
      - run: cargo build -p hsm-fw --target thumbv6m-none-eabi --release

  go-host:
    name: go host (vet, test)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
      - working-directory: host
        run: |
          go vet ./...
          go test ./...

  differential:
    name: differential (HSM cert == x/crypto/ssh)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
      - working-directory: tests/differential
        run: go test ./...
