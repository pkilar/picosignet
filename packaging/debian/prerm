#!/bin/sh
# picosignet prerm: stop the bridge before the binary/unit is removed.
set -e

if [ "$1" = "remove" ] && [ -d /run/systemd/system ]; then
	systemctl --system stop picosignet-bridge.service >/dev/null 2>&1 || true
fi

exit 0
